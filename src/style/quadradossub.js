import styled from "styled-components";

export const Subquadrados = styled.div`
position: absolute;
width: 302px;
height: 302px;

left: ${props=> props.left};
top: ${props=> props.top};

background: ${props=> props.color};







`
